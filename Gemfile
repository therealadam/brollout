source "http://rubygems.org"

# Specify your gem's dependencies in brollout.gemspec
gemspec

group(:development) do
  # Because activesupport has a jacked dependency?
  gem 'i18n'
end

group(:test) do
  gem 'rspec', '~> 2.6.0'
end
